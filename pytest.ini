[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Mark tests that require Langflow to be installed
markers =
    langflow: tests that require Langflow framework (integration tests)
    unit: unit tests that don't require external dependencies
    slow: tests that take longer to run
